<div class="col-md-3">
    
    <div class="search-open" style="display: block;">
        <div class="input-group ">
            <input type="text" class="form-control" placeholder="Filtro offerte/eventi ">
            <span class="input-group-btn">
                <button class="btn-u" type="button">Go</button>
            </span>
        </div>
    </div>
    <hr>

    <% if(logged) { %>
    <button id="addPromotionButton" class="btn-u" type="button"> Nuova Promozione</button>
    <hr>
    <% } %>
    
    <div id="promoList"></div>

<!--
    <hr>

    <div class="form-group">
        <div class="row">
            <div class="col-md-8">
                <div id="datetimepicker12"></div>
            </div>
        </div>
    </div>
-->

</div>

<script id="promo-template" type="text/x-handlebars-template">
    {{#each promos}}
    <div class="tag-box tag-box-v3 margin-bottom-40">
        <h2><a href="/activities/{{this.idcontent}}/promotions/{{this._id}}/">{{this.name}}</a></h2>
        <small style="color:green">dal {{this.startDate}} <br> al {{this.endDate}}</small>
        <p>{{this.description}}</p>
    </div>
    <hr>
    {{/each}}
</script>